<?xml version="1.0" encoding="UTF-8"?>
<service-eca xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="http://ofbiz.apache.org/dtds/service-eca.xsd">

    <eca service="changePromoSettlementStatus" event="commit">
    	<action mode="sync" service="sendNotifyWhenCreatePromoSettlement"/>
    </eca>

<!--<eca service="rejectDistributor" event="commit">
        <action mode="sync" service="rejectPartyRelationDms"/>
    </eca> -->

    <eca service="rejectAgent" event="commit">
        <action mode="sync" service="rejectPartyRelationDms"/>
    </eca>

    <eca service="rejectMTCustomer" event="commit">
        <action mode="sync" service="rejectPartyRelationDms"/>
    </eca>

    <!--<eca service="rejectMTConsignee" event="commit">-->
        <!--<action mode="sync" service="rejectPartyRelationDms"/>-->
    <!--</eca>-->
</service-eca>
